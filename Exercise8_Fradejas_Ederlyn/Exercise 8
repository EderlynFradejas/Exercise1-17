1.How many unique salary packages does our company, bignet, offer?
12 unique salary.
SELECT DISTINCT salary FROM employee_data ORDER BY salary DESC;
2.How many distinct first names do we have in our database?
There are 21 distinct first names
SELECT COUNT(DISTINCT f_name) AS distinct_first_names FROM employee_data;
